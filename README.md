### 结构
```
collect                           # 项目根目录
├── docker-compose.yml            # Docker 编排：EMQX + 采集
├── Dockerfile                    # 镜像文件构建
├── requirements.txt              # Python 依赖清单 
├── main.py                       # 入口  
├── start.py                      # 启动初始化  
   
```
